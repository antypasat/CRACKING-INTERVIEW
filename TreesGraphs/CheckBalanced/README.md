# 4.4 Check Balanced

Check if binary tree is balanced (heights of subtrees differ by at most 1).

**Solution:** Check height recursively, return -1 if unbalanced.

**Complexity:** O(n) time, O(h) space
