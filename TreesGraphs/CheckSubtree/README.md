# 4.10 Check Subtree

Check if T2 is a subtree of T1 (identical structure from some node).

**Solutions:**
1. Recursive matching at each node
2. Pre-order string comparison

**Complexity:** O(n*m) time, O(h) space
